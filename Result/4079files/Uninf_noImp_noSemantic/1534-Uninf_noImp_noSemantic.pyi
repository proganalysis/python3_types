import pandas as pd
from benchmark_base import Benchmark
from typing import Any

N_SAMPLES: Any
N_RUNS: int

class BenchmarkConcordance(Benchmark):
    @staticmethod
    def load_data(): ...
    def gen_more_data(self, train: pd.DataFrame, predict: pd.DataFrame, result: pd.DataFrame) -> Any: ...
    @staticmethod
    def gen_similar_df(df: pd.DataFrame, data_types: list) -> pd.DataFrame: ...
    @staticmethod
    def check_concordance(submission: Any, clusters: Any, ids: Any): ...
    def benchmark(self) -> None: ...

def main() -> None: ...
