from simplest.transaction import SignedTransaction as SignedTransaction
from typing import Any, Iterable

class Block:
    id: str = ...
    secret: int = ...
    trxs: Any = ...
    prev_block: Any = ...
    def __init__(self, trxs: Iterable[SignedTransaction], prev_block: Block) -> None: ...
    def validate(self) -> bool: ...
    @staticmethod
    def create_id(trxs: Iterable[SignedTransaction], prev_block: Block, secret: int) -> str: ...
    def __str__(self) -> str: ...
