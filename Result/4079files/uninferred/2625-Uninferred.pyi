from errbot import BotPlugin
from typing import Any

logger: Any

class Merge(BotPlugin):
    def activate(self) -> None: ...
    def setup_repos(self) -> None: ...
    def get_configuration_template(self) -> str: ...
    def check_configuration(self, configuration: typing.Mapping) -> None: ...
    def merge(self, msg: errbot.backends.base.Message, project_name: str, branch_name: str) -> str: ...
    def get_project_root(self, project_name: str) -> str: ...
    def validate_branch(self, branch_name: str, project_root: str) -> Any: ...
    @staticmethod
    def git_merge_branch_to_develop(project_root: str, branch_name: str, author: str, invoking_user: str) -> Any: ...
    @staticmethod
    def git_push_develop_to_origin(project_root: str) -> Any: ...
    @staticmethod
    def git_delete_branch(project_root: str, branch_name: str) -> Any: ...
    @staticmethod
    def git_get_branch_author(project_root: str, branch_name: str) -> str: ...
    @staticmethod
    def run_subprocess(args: list, cwd: str=...) -> Any: ...
