from coalib.bearlib.abstractions.SectionCreatable import SectionCreatable
from typing import Any

class SpacingHelper(SectionCreatable):
    DEFAULT_TAB_WIDTH: int = ...
    tab_width: Any = ...
    def __init__(self, tab_width: int=...) -> None: ...
    def get_indentation(self, line: str) -> Any: ...
    def replace_tabs_with_spaces(self, line: str) -> Any: ...
    def yield_tab_lengths(self, input: str) -> Any: ...
    def replace_spaces_with_tabs(self, line: str) -> Any: ...
