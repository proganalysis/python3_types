from typing import Dict, List
from vhpi.api.types import BackupRoot as BackupRoot, Interval as Interval, IntervalDuration as IntervalDuration, Job, KeepAmount as KeepAmount, Settings as Settings

def _initial_job_validation_routine(job_cfg: dict) -> bool: ...
def _is_snap_due(interval: Interval, intervals: Dict[Interval, int], timestamp: str) -> bool: ...
def _load_timestamps(backup_root: BackupRoot, intervals: Dict[Interval, IntervalDuration]) -> Dict[Interval, str]: ...
def _get_due_snapshots(timestamps: Dict[Interval, str], snapshots: Dict[Interval, KeepAmount], intervals: Dict[Interval, IntervalDuration]) -> List[Interval]: ...
def _init_job(job_cfg: dict, settings: Settings, init_time: float) -> Job: ...
def _duty_check_routine(job: Job) -> bool: ...
def run(job_cfg: dict, settings: Settings) -> None: ...
