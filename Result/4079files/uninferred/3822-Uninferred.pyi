from Cogs.Utils.custom_bot import CustomBot as CustomBot
from discord import FFmpegPCMAudio as FFmpegPCMAudio, PCMVolumeTransformer as PCMVolumeTransformer, VoiceChannel as VoiceChannel
from discord.ext.commands import Context as Context
from typing import Any

class Music_Commands:
    bot: Any = ...
    queue: Any = ...
    def __init__(self, bot: CustomBot) -> None: ...
    async def join(self, ctx: Context, channel: VoiceChannel) -> Any: ...
    async def play(self, ctx: Context, url: str) -> Any: ...
    async def volume(self, ctx: Context, volume: int) -> Any: ...
    async def stop(self, ctx: Context) -> Any: ...

def setup(bot: Any) -> None: ...
